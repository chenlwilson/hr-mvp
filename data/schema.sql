-- psql -s quickdraw

CREATE TABLE draw_0s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_1s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_2s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_3s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_4s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_5s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_6s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_7s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_8s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_9s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);

CREATE TABLE draw_10s(
  id SERIAL PRIMARY KEY,
  word varchar(20) NOT NULL,
  drawing json NOT NULL
);